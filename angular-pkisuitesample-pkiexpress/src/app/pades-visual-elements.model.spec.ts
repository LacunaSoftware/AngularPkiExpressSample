import { PadesVisualElements } from './pades-visual-elements.model';

describe('PadesVisualElements', () => {
  it('should create an instance', () => {
    expect(new PadesVisualElements()).toBeTruthy();
  });
});
